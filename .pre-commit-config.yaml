# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
        exclude: '.*\.md$'
      - id: end-of-file-fixer
        exclude: '.*\.md$'
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: check-json
      - id: detect-private-key

  - repo: https://github.com/rhysd/actionlint
    rev: v1.7.7
    hooks:
      - id: actionlint

    # Note: shellcheck cannot directly parse YAML; actionlint extracts workflow
    # shell blocks and calls shellcheck when available.
  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.11.0.1
    hooks:
      - id: shellcheck
        # Match by detected shell file type (extensions or shebang)
        types: [shell]
        args: ['-x']

  - repo: https://github.com/codespell-project/codespell
    rev: v2.4.1
    hooks:
      - id: codespell
        additional_dependencies:
          - tomli

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.13.1
    hooks:
      # Run the linter.
      - id: ruff-check
        args: [--fix]
      # Run the formatter.
      - id: ruff-format

  # - repo: https://github.com/pre-commit/mirrors-eslint
  #   rev: v9.36.0
  #   hooks:
  #     - id: eslint
  #       language_version: 18.12.1
  #       # Ensure ESLint uses the project's ESM flat config located in the frontend
  #       # directory. Run from repo root but point to the frontend config file.
  #       entry: eslint --config frontend/eslint.config.js
  #       args: ['--fix']
  #       files: '^frontend/.*\.(js|jsx|ts|tsx)$'
  #       additional_dependencies:
  #         - '@eslint/js'
  #         - 'eslint-plugin-react'
  #         - 'eslint-plugin-unused-imports'
  #         - '@typescript-eslint/parser'
  #         - '@typescript-eslint/eslint-plugin'
  #         - 'eslint-config-prettier'
  #         - 'prettier'

  - repo: https://github.com/rbubley/mirrors-prettier
    # https://github.com/JoC0de/pre-commit-prettier
    rev: v3.6.2
    hooks:
      - id: prettier
        args: ['--write']
        files: \.(js|jsx|ts|tsx|json|css|md|html)$

  - repo: local
    hooks:
      - id: npm-install
        name: Run npm install if package.json or package-lock.json changed
        entry: bash -c 'npm install'
        language: system
        files: ^(package\.json|package-lock\.json)$
        pass_filenames: false

ci:
  autofix_commit_msg: |
    [pre-commit.ci] auto fixes from pre-commit hooks
  autofix_prs: true
  autoupdate_commit_msg: '[pre-commit.ci] pre-commit autoupdate'
  autoupdate_schedule: weekly
  skip: []
  submodules: false
